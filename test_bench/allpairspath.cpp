#include <stdio.h>
#include <math.h>
#include <climits>
#define INF INT_MAX

int main(){
	int n, m, numQueries;
	scanf("%d %d %d", &n, &m, &numQueries);
	while((n + m + numQueries) != 0){


		scanf("%d %d %d", &n, &m, &numQueries);
		
	}


}

